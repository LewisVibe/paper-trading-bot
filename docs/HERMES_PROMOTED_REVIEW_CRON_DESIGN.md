# Superseded Hermes Promoted Review Cron Design

This file is kept as a legacy pointer only.

The canonical future-only design for a possible promoted-review refresh cron job
is:

`docs/HERMES_PROMOTED_REVIEW_REFRESH_CRON_DESIGN.md`

Use the canonical verifier:

```powershell
python scripts\verify_hermes_promoted_review_refresh_cron_design.py
```

Do not maintain a second command sequence or approval checklist here. No
promoted-review refresh cron job is currently created or approved. This pointer
does not approve scheduling, execution, orders, paper execution, live trading, or
any cron changes.
